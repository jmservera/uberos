# colcon default settings for the UbeROS ROS workspace.
# symlink-install lets Python edits take effect without a full rebuild.
build:
  symlink-install: true
  event-handlers:
    - console_direct+
test:
  event-handlers:
    - console_direct+
